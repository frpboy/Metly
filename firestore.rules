rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // TEST MODE: Allow public read/write (for development only)
    // WARNING: Replace with production rules before launching!
    match /{document=**} {
      allow read, write: if true;
    }

    // PRODUCTION RULES (Uncomment when Auth is ready)
    // match /users/{userId} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    //
    //   match /watchlist/{metalId} {
    //     allow read, write: if request.auth != null && request.auth.uid == userId;
    //   }
    //   
    //   match /portfolio/{itemId} {
    //     allow read, write: if request.auth != null && request.auth.uid == userId;
    //   }
    // }
  }
}
